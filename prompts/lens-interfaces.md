## Interfaces Lens

Review interface quality:

1. Naming consistency: same concepts use same names across the stack?
2. Type safety: missing types, any usage, unsafe casts, missing Python type hints
3. API contract drift: frontend types match backend responses?
4. Unclear signatures: too many params, boolean flags, unclear returns
5. Missing or misleading types
