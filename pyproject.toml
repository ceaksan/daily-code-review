[project]
name = "dnm-audit"
version = "0.1.0"
description = "Daily code health audit CLI"
requires-python = ">=3.12"

[project.scripts]
dnm-audit = "dnm_audit.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
